name: Build Kernel Module (LineageOS SM8650)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: 检出驱动源码
        uses: actions/checkout@v4

      - name: 安装编译工具链
        run: |
          sudo apt update
          sudo apt install -y git make flex bison bc rsync cpio \
            gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu \
            libc6-dev-arm64-cross \
            libelf-dev libdw-dev build-essential

      - name: 克隆 LineageOS 内核源码（SM8650）
        run: |
          git clone --depth 1 --branch lineage-21 \
            https://github.com/LineageOS/android_kernel_xiaomi_sm8650.git kernel

      - name: 应用手机实际内核配置
        run: |
          cp config kernel/.config
          cd kernel
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- olddefconfig

      - name: 将驱动源码复制到内核树
        run: |
          mkdir -p kernel/drivers/misc/kernrw
          cp drivers/misc/kernrw/kernrw.c kernel/drivers/misc/kernrw/
          cp drivers/misc/kernrw/Makefile kernel/drivers/misc/kernrw/
          echo "obj-m += kernrw.o" > kernel/drivers/misc/kernrw/Makefile

      - name: 编译内核模块
        run: |
          cd kernel
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- modules_prepare
          make ARCH=arm64 CROSS_COMPILE=aarch64-linux-gnu- \
            M=drivers/misc/kernrw \
            KBUILD_MODPOST_WARN=1 \
            KBUILD_CFLAGS="-Wno-error" \
            modules

      - name: 上传编译好的模块
        uses: actions/upload-artifact@v4
        with:
          name: kernrw-ko
          path: kernel/drivers/misc/kernrw/kernrw.ko
